<%= semantic_form_for [@problem, @piece] do |f| %>
  <%= f.inputs do %>
    <%= f.input :source, :label => '소스 코드', :as => :file %>
    <%= f.input :comment, :label => '남기는 말' %>
  <% end %>

  <%= f.buttons do %>
    <%= f.commit_button :label => '실행' %>
  <% end %>
<% end %>
